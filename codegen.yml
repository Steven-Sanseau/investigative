overwrite: true
schema: 'https://api.tanzania-investigative.space/graphql'
documents: 'src/graphql/**/*.ts'
generates:
  src/generated/graphql.ts:
    plugins:
      - typescript
      - fragment-matcher
      - typescript-operations
      - typescript-graphql-request
